export const metadata = {
  title: "Solar Products — Panels, Inverters & Mounting Systems",
  description:
    "Tier-1 solar panels, string inverters, mounting structures and balance-of-system components we install — specifications, warranties and brand options.",
  alternates: { canonical: "/products" },
};

export default function ProductsLayout({ children }) {
  return children;
}
