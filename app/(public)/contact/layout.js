export const metadata = {
  title: "Contact Us — Free Solar Consultation & Quote",
  description:
    "Get a free rooftop solar consultation and quote. Call +91 94229 80148 or send an enquiry — our team responds within 24 hours.",
  alternates: { canonical: "/contact" },
};

export default function ContactLayout({ children }) {
  return children;
}
