{
  "permissions": {
    "allow": [
      "Bash(python -)",
      "Bash(git mv *)",
      "Bash(sed -i 's/export default function middleware\\(request\\)/export default function proxy\\(request\\)/' proxy.js)"
    ]
  }
}
